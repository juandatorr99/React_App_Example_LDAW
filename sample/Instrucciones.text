
1. Primero se construye con el siguiente comando: docker build -t sample:dev .
2. Se corre el docker en el puerto 300 con el siguiente comando:  docker run -v ${PWD}:/app -v /app/node_modules -p 3000:3000 --rm sample:dev